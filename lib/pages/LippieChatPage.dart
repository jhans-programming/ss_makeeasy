import 'package:flutter/material.dart';

class LippieChatPage extends StatelessWidget {
  const LippieChatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
