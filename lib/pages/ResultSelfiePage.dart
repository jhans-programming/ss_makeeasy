import 'package:flutter/material.dart';

class ResultSelfiePage extends StatelessWidget {
  const ResultSelfiePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
